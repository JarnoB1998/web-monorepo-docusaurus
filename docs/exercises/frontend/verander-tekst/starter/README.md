# Verander tekst (starter)

Gebruik Node.js 22.12 of nieuwer.

```bash
npm install
npm run dev
```

Typecontrole: `npm run typecheck`. Productiebuild: `npm run build`.

Lees de opdracht in de cursus.
