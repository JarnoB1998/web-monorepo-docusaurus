export const printStuff = (amount: number, text: string): void => console.log(`Hello ${text}, you are number ${amount}`);

export const twoDArray = (element1: string, element2: string): string[] => [element1, element2];

export const numberToString = (number: number): string => `${number}`;
